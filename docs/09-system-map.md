# 09. Блок-схема системы: листы и процессы

Снимок 02.07.2026. Карта того, как связаны между собой документы (листы) и процессы всей
системы бордов: источники → провенанс → канон → процессная модель → рабочие инструменты.
Диаграммы — Mermaid, рендерятся в веб-интерфейсе GitHub.

## 1. Общая карта системы (слои)

Путь знания: PDF с Диска → заметки-источники → канон → производные артефакты → инструменты,
которыми пользуются. Стрелка = «служит источником / порождает».

```mermaid
flowchart TB
  subgraph SRC["ИСТОЧНИКИ"]
    PDF1["Google Drive: 1.pdf<br/>MMS Том I · Business Architecture"]
    PDF2["Google Drive: 2.pdf<br/>MMS Том II · Functional Specification"]
    REF["reference/<br/>V4.1 «петли ценности» · чек-лист Федоренко · схема процесса"]
  end

  subgraph PROV["ПРОВЕНАНС · vault/10-sources/"]
    SN1["mms-tom-1-business-architecture.md"]
    SN2["mms-tom-2-functional-specification.md"]
  end

  subgraph CANON["КАНОН · docs/"]
    CTX["01–07: стратегия · продукт · custdev ·<br/>операционка · риски · данные · принципы"]
    B08["08-board-system.md<br/>обзор + критический разбор"]
    BRD["boards/ — модуль системы бордов<br/>(00–04, 10–16, 90)"]
  end

  subgraph DERIV["ПРОИЗВОДНЫЕ"]
    PRM["prompts/bpmn-decomposition.md<br/>исполнимый промпт"]
    BPMN["bpmn/ — процессная модель<br/>(README, 01–05, P-MGMT-01.bpmn)"]
  end

  subgraph TOOLS["ИНСТРУМЕНТЫ · GitHub Pages"]
    T1["s1.html — рабочий борд<br/>всех направлений (localStorage)"]
    T2["index.html — борд v0.1"]
    T3["marketing.html — Marketing OS v0.2"]
    T4["mms-board-system.html —<br/>визуализатор канона"]
  end

  PDF1 -->|"выгрузка 27.06"| SN1
  PDF2 -->|"выгрузка 27.06<br/>(оборвана на §10.11 — GAP)"| SN2
  SN1 --> B08
  SN2 --> B08
  SN1 --> BRD
  SN2 --> BRD
  REF -->|"V4.1: зрелость · Федоренко: стадии"| B08
  CTX -->|"стадия · узкое место ·<br/>правило двух потоков"| B08
  B08 <--> BRD
  BRD -->|"источник требований"| PRM
  PRM -->|"исполнение промпта"| BPMN
  BPMN -.->|"сверка процессов с каноном"| BRD
  BRD -->|"канон интерфейса"| T4
  BRD -->|"04-stage0-mvp → инструмент"| T1
  PDF2 -.->|"прототипы по спецификации"| T2
  T3 -.->|"MOS-промпт + борд v0.1"| T2
```

## 2. Связи листов внутри модуля `docs/boards/`

Каждый борд опирается на каркасное ядро (00 каркас · 01 KPI · 02 роли · 03 цикл решений);
`08` — точка входа и реестр; `04-stage0` — мост к стадии, на него ведут стадийные баннеры
всех L1-бордов; `90` собирает приёмку. Пунктир — ссылка во внешний канон.

```mermaid
flowchart LR
  B08["../08-board-system.md<br/>обзор · реестр · критика"]

  subgraph CORE["Каркасное ядро"]
    F00["00-framework<br/>каркас борда"]
    F01["01-kpi-framework<br/>KPI L0–L3 · реестр KPI v0"]
    F02["02-roles-access<br/>RBAC · владельцы"]
    F03["03-decision-loop<br/>цикл · alerts · PB-001…005"]
  end

  F04["04-stage0-mvp<br/>стартовый контур S0–S2 ·<br/>каталог KPI v0 · решения основателя"]

  subgraph BOARDS["Семь бордов"]
    B10["10-owner (L0)"]
    B11["11-finance"]
    B12["12-marketing"]
    B13["13-sales"]
    B14["14-operations"]
    B15["15-expert-network"]
    B16["16-product"]
  end

  B90["90-acceptance<br/>приёмка MMS + S0–S1"]

  subgraph EXT["Внешний канон docs/"]
    X1["01-strategy<br/>два потока"]
    X2["02-product<br/>продукты · тарифы"]
    X4["04-operations<br/>узкое место · V4.1 · Федоренко"]
    X5["05-risk-compliance"]
    X6["06-data-tools-payments"]
  end

  B08 <--> CORE
  B08 <--> BOARDS
  B08 --> F04
  B08 --> B90

  BOARDS -->|"каркас · KPI · цикл"| CORE
  BOARDS -.->|"стадийные баннеры"| F04
  F04 --> B10
  F04 --> B14
  B90 --> BOARDS

  B11 --> B10
  B12 --> B13
  B13 --> B11
  B14 <--> B15
  B15 --> B11
  B16 --> B11
  B16 --> B15

  F00 -.->|"правило потоков"| X1
  F02 -.-> X5
  F03 -.-> X4
  F04 -.-> X4
  B12 -.-> X6
  B14 -.-> X4
  B15 -.-> X4
  B16 -.-> X2
```

## 3. Процессы и их листы

Как процессы связаны между собой и какой лист какой процесс специфицирует (подписи в узлах).
Ядро всего — цикл решений; борды поставляют в него alerts; два потока пересекаются только у
Owner; узкое место наблюдается журналом S0 и выводится на рабочий борд.

```mermaid
flowchart TB
  subgraph VS["Потоки ценности · bpmn/02 · два pool — НЕ смешивать"]
    P1["Поток-1 · Founder Premium (HNW)<br/>личные продажи · ₽ и £ раздельно"]
    P2["Поток-2 · Platform<br/>Трафик → Лид → Скрининг → Оплата → Выдача"]
    BN["★ Узкое место: экспертная проверка отчёта<br/>+ Report Queue · ../04-operations"]
    P2 --> BN
  end

  subgraph DATA["Конвейер данных · bpmn/03 · P-DATA-01"]
    EV["Business Event"] --> RAW["Raw Data"] --> OBJ["Business Objects"]
    OBJ --> KPIC["KPI Catalog · SSOT<br/>(boards/01-kpi-framework)"]
    KPIC --> DASH["Борды L0/L1<br/>(boards/10–16)"]
  end

  subgraph CYCLE["Цикл решений · boards/03 + bpmn/01 · P-MGMT-01"]
    AL["Alert<br/>(Information/Warning/Critical/Emergency)"]
    DR["Drill Down · SP-DRILL"]
    RC["Root Cause · SP-ROOTCAUSE"]
    PB["Playbook PB-001…005<br/>(bpmn/05 — templated)"]
    DEC["Решение — всегда человек"]
    EX["Execution"]
    VER["Verification · SP-VERIFY"]
    KN["Knowledge Base"]
    AL --> DR --> RC --> PB --> DEC --> EX --> VER
    VER -->|"достигнут"| KN
    VER -->|"нет — повторный цикл"| DR
  end

  AI["AI Copilot — ассистент:<br/>объясняет · прогнозирует · рекомендует<br/>НЕ решает (Том I, Принцип №11)"]
  OWNER["Owner: консолидация двух потоков<br/>(boards/10-owner · P-OWNER-01)"]

  subgraph STAGE["Стартовый контур · boards/04-stage0 · сейчас"]
    S0["S0: журнал очереди<br/>5 событий → WIP · p90 · срок · правки"]
    S1["S1: рабочий борд s1.html<br/>воронка + деньги + все направления"]
    DJ["Журнал решений<br/>дата · данные · решение · проверка"]
    S0 --> S1 --> DJ
  end

  P1 ==>|"метрики потока-1"| OWNER
  P2 ==>|"метрики потока-2"| OWNER
  P2 --> EV
  BN -->|"наблюдаемость (V4.1)"| S0
  KPIC -->|"порог превышен"| AL
  DASH --> OWNER
  DASH -->|"отклонение"| AL
  AI -.-> DR
  AI -.-> PB
  KN -.->|"обучение playbook"| PB
  DJ -.->|"ручное исполнение цикла"| CYCLE
  S1 -.->|"алерты по порогам основателя"| AL
```

## Как читать

- **Слой 1** отвечает «откуда что взялось и куда идёт»: любой факт инструмента прослеживается
  через канон к PDF-источнику с датой (правило данных проекта).
- **Слой 2** отвечает «какой лист открывать следом»: борды не читаются в отрыве от ядра
  00–03, а мостом к практике служит `04-stage0-mvp`.
- **Слой 3** отвечает «что происходит в работе»: события → KPI → alert → цикл решений →
  знание; на текущей стадии тот же цикл исполняется вручную связкой журнал S0 → борд S1 →
  журнал решений.

Инварианты, видимые на схеме: два потока пересекаются только у Owner; AI нигде не владеет
решением; узкое место — единственная точка, где клиентский поток входит в операционное
наблюдение S0; каталог KPI — единственный источник истины между данными и бордами.

## Связи

- Обзор системы — `08-board-system.md`; детальный канон — `boards/`; процессы — `bpmn/`.
- Инструменты: `s1.html` (рабочий борд), `mms-board-system.html` (визуализатор канона).
