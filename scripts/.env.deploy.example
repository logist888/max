# Скопируйте в scripts/.env.deploy и впишите реальные значения.
# Файл .env.deploy в .gitignore — в репозиторий не попадёт.
FTP_SERVER=31.31.197.39
FTP_USERNAME=u3591005
FTP_PASSWORD=ВПИШИТЕ_НОВЫЙ_ПАРОЛЬ
# Корень сайта на сервере — узнаётся командой: ./scripts/deploy-ftp.sh --discover
FTP_REMOTE_DIR=
SITE_URL=https://vuz-navigator.ru
# true — удалять на сервере файлы, которых больше нет в сборке.
# Включать только после успешного --dry-run.
FTP_DELETE_STALE=false
